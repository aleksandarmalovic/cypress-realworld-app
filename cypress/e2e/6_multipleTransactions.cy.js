describe('Make multiple transactions and validate Amount filter', () => {


    it('Choosse random user', () => {
        //text
    })

    it('validate message for dot(.) input', () => {
        //text
    })

    it('validate that amount field can be field only with real numbers', () => {
        //text
    })

    it('Go to /personal and validate transaction', () => {
        //text
    })

})